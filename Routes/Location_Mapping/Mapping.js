"use strict";
const config = require("../../Config");
const apiCommonController = require("../../Utility/APICommonController");
const providerDestination = require("../../Utility/APICommonController")

module.exports = async function (app) {

    app.get("/LocationMapping", async (request, response, next) => {
        // Log file name and path declaration
    });
}